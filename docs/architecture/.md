Do not place the OIDC provider and GitHub IAM roles inside the ordinary dev infrastructure state.

Use this :

bootstrap/github-oidc

This prevents a normal development teardown from deleting the authentication system required by future pipelines.

meeps-terraform-state-...
    └── Terraform state and .tflock objects

meeps-users-posts-api-artifacts-...
    └── Application ZIP files and checksums
