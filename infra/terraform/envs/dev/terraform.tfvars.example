# Optional root-level overrides.
# Resource-specific settings live with the child module that owns each resource.

aws_region  = "eu-west-2"
project     = "meeps"
owner       = "meeps"
environment = "dev"
